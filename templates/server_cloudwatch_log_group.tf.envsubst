resource "aws_cloudwatch_log_group" "server" {
  name              = "${var.project}-${var.environment}-server"
  retention_in_days = var.server.aws_cloudwatch_log_group_retention_in_days
  tags = {
    "Name" = "${var.project}-${var.environment}-server"
  }
}
