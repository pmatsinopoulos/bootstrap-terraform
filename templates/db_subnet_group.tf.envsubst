resource "aws_db_subnet_group" "db" {
  name       = "${var.project}-${var.environment}-db-subnet-group-main"
  subnet_ids = [for k, v in var.vpc_subnets : aws_subnet.main[k].id]

  tags = {
    "Name" = "${var.project}-${var.environment}-db-subnet-group-main"
  }
}
